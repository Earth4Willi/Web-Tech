[build]
  base = "artifacts/ict233-study-guide"
  command = "pnpm install --frozen-lockfile && pnpm build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20.19.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200